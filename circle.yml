dependencies:
  override:
    - pip install -r requirements.txt
    - pip install coverage coveralls nose

test:
  pre:
    - python setup.py install
  override:
    - make alltests
    - nosetests --with-coverage --cover-package=mailpile
  post:
    - coverage report
